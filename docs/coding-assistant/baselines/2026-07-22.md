# Coding Assistant Baseline — 2026-07-22

## Scope

Initial repository rules, environment diagnostics, verification routing, and
the ten-case evaluation rubric.

## Automated evidence

`./scripts/coding-assistant/verify.sh all` produced:

- Common shell and rule checks passed.
- Project registry tests: 3 passed, 0 failed.
- Project Runner tests: 1 passed, 0 failed.
- TypeScript and Vite production build passed; 6,913 modules transformed.
- `cargo check -p xai-grok-pager-bin` passed using an external target.
- Four existing future-compatibility warnings remained in the web-search
  client and were not changed because they were outside this task.

The current process did not provide `MIMO_API_KEY` or `DEEPSEEK_API_KEY`, so
model-backed, browser, permission, and owning-system readback cases were not
counted as passed. Existing user changes were preserved.
