# Stillpoint 页面业务逻辑与交互审查

日期：2026-07-22

## 审查范围

- 页面：Stillpoint Personal Coding IDE 本地工作台首页。
- 核心目标：在真实工作目录与 ACP 会话边界内，新建或恢复编程会话，提交任务，查看执行活动，并处理 Agent 的提问、计划和工具权限。
- 主要流程：启动与连接 → 选择项目/历史会话 → 输入任务与附件 → Agent 执行 → 权限/问题/计划响应 → 完成或错误恢复。
- 实际运行地址：`http://127.0.0.1:5177/`。
- 当前运行限制：本次进程未配置 `MIMO_API_KEY` / `DEEPSEEK_API_KEY`，因此可验证连接失败与恢复体验、项目流程、响应式和键盘行为；不能完成真实模型回复和真实权限弹窗验收。

## 状态模型

- Bridge：`offline → connecting → connected | error`。
- 会话：未就绪 → 新建/恢复中 → 可输入 → 运行中 → 完成/失败/取消。
- 项目：列表加载 → 新建/编辑详情 → 添加来源 → 保存中 → 成功/失败。
- Agent 等待用户：权限、问题、计划三类互斥交互卡片。
- 响应式导航：桌面 300px 完整栏 ↔ 72px 窄轨；移动端全屏导航 ↔ 主内容。

## 发现与处理（严重程度 × 用户影响）

1. **P0 — 权限范围可能被静默扩大（已修复）**
   - 原行为：界面固定显示“允许一次”，代码在缺少 `allow_once` 时会选择 `allow_always`；拒绝操作也存在同类回退。
   - 影响：用户可能在不知情时授予持久权限。
   - 修复：严格按 Agent 返回的 `optionId` 提交，并按实际类型显示“允许一次 / 始终允许 / 拒绝 / 始终拒绝”；不再跨权限范围回退。

2. **P1 — 重复发送与草稿丢失（已修复）**
   - 原行为：点击发送后先清空输入和附件；React 状态更新前的快速双击可并发发起两次请求，运行中再次发送也会清空草稿。
   - 影响：重复执行编程任务，或丢失用户尚未成功发送的内容。
   - 修复：增加同步提交锁；只有请求被接受时才清空草稿；运行中发送按钮禁用；终止事件和断线都会释放提交锁。

3. **P1 — 连接失败只有禁用控件，没有原因与恢复路径（已修复）**
   - 证据：`webui/audit/01-startup-unavailable.png`。
   - 原行为：页面显示正常欢迎语，但输入、附件和发送全部不可用，错误只写入不可见事件列表。
   - 修复：新增连接中、会话准备中、连接失败和未连接状态卡；提供原因、`aria-live` 状态和“重新连接”操作。
   - 修复后证据：`webui/audit/05-mobile-error-recovery-fixed.png`、`webui/audit/06-desktop-error-recovery-fixed.png`。

4. **P1 — 新建会话失败会锁死原会话（已修复）**
   - 原行为：创建失败后无条件设置 `sessionReady=false`，原会话仍存在但无法继续输入。
   - 修复：失败时恢复原会话可用状态，并在对话中显示“新会话创建失败，原会话已保留”。启动初始化、会话列表和新建会话也增加了有限超时。

5. **P1 — 移动端侧栏缺少可见出口（已修复）**
   - 证据：`webui/audit/02-mobile-sidebar-open.png`。
   - 原行为：收起按钮默认透明，只在 hover 或键盘焦点时显示；触屏设备没有可靠 hover。
   - 修复：移动端始终显示收起按钮。
   - 修复后证据：`webui/audit/04-mobile-sidebar-fixed.png`。

6. **P2 — 项目保存可能重复提交，失败反馈不可见（已修复）**
   - 原行为：创建/保存按钮没有同步防重、保存中状态或表单内错误；来源步骤无法返回详情。
   - 修复：增加同步保存锁、按钮 loading/disabled、表单内错误和“上一步”；返回后项目名称与指令草稿仍保留。
   - 证据：`webui/audit/07-project-source-keyboard-fixed.png`。

7. **P2 — 焦点与键盘操作不完整（已修复）**
   - 原行为：收起侧栏后焦点随已卸载按钮丢失；项目拖放区和连接器卡片只能鼠标点击；持续动画未响应减少动态效果偏好。
   - 修复：侧栏状态切换后把焦点移动到对应恢复控件；拖放区和连接器卡片增加 button 语义、Tab、Enter 和 Space；增加 `prefers-reduced-motion`。
   - 验证：移动端收起后焦点为“展开侧栏”，恢复后焦点为“收起侧栏”；桌面窄轨宽 72px，无横向溢出。

## 流程健康度

1. **启动与连接：改善后可恢复** — 错误原因和重试入口可见；健康连接受本次凭据缺失限制，未完成模型级验收。
2. **项目与历史导航：健康** — 项目树展开/收起、移动端导航出口和焦点恢复通过。
3. **项目创建：健康** — 两步表单、返回编辑、键盘可达和防重复保存已验证；未创建审查用临时项目。
4. **任务提交：源码与构建验证通过** — 防重与草稿保护已落实；真实模型请求因缺少凭据未执行。
5. **Agent 等待用户：安全逻辑已修复** — 权限选项不再升级；真实权限/问题/计划弹窗未能由当前 Agent 触发。
6. **错误与恢复：健康** — 连接失败状态在桌面和移动端可见，重试按钮可操作，控制台无警告或错误。

## 验证结果

- `./scripts/coding-assistant/doctor.sh`：环境检查通过；模型密钥缺失为已记录限制。
- `./scripts/coding-assistant/verify.sh webui`：通过。
- `npm run test:registry`：3/3 通过。
- `npm run test:runner`：1/1 通过。
- `npm run build`：通过，6913 modules transformed。
- `git diff --check`：通过。
- 浏览器：1487×1058 与 390×844 均无横向溢出；控制台 warnings/errors 为 0。

## 仍需产品确认

1. 自动化、技能和连接器目前是本地草稿或引导入口，不是完整调度/安装能力。需要决定是继续保留为预览入口，还是在能力接通前隐藏。
2. 项目详情关闭时是否需要“未保存草稿”确认或自动恢复。当前新增了“上一步”避免步骤间内容丢失，但用户主动关闭对话框仍会放弃草稿。
3. 是否允许用户在 Agent 离线时先编辑任务草稿。当前保持原产品规则：会话未就绪时输入框禁用。
4. “始终允许 / 始终拒绝”是否应在 WebUI 中开放。当前只会在 Agent 明确返回对应选项时显示，未擅自隐藏协议能力。

## 证据限制

- 截图可确认可见状态、布局、焦点出口和可操作控件，但不能单独证明完整 WCAG 合规。
- 当前无法验证真实 MiMo/DeepSeek 回复、长时间运行、权限弹窗和网络中断中的服务器端最终状态；需要在批准的密钥环境中补一次端到端验收。

## 交互优化复审（同日第二轮）

### 本轮发现与处理

1. **P1 — 操作反馈写入状态但用户看不到（已修复）**
   - 模型模式、授权、附件错误和连接错误过去主要进入内部 `events`，没有即时可见反馈。
   - 现在用自动消失、可手动关闭的状态提示反馈最近操作；错误使用 `alert`，普通操作使用 `status`。右侧面板同时增加“最近操作”，提供可追溯记录。
   - 证据：`webui/audit/interaction-05-feedback-clean.jpg`、`webui/audit/interaction-04-optimized-panel.jpg`。

2. **P1 — 长回复推动整个页面，输入区无法稳定停留（已修复）**
   - 原聊天区域没有形成独立滚动容器，内容变长后页面整体增高，自动跟随逻辑无法生效。
   - 现在主会话固定为视口高度，回复区独立滚动；用户停留在底部时继续跟随生成，主动上翻后停止抢滚动，并显示“跳到最新消息”。
   - 证据：`webui/audit/interaction-06-jump-to-latest.jpg`。

3. **P1 — Agent 离线时无法准备草稿，本地命令也被误锁（已修复）**
   - 输入与附件现在允许离线准备；普通任务保留草稿并提示“连接 Agent 后发送”。
   - `/help`、`/status`、`/model`、`/theme`、`/clear` 等本地命令可在离线状态执行，不会伪装成模型能力。

4. **P2 — 右侧面板缺少完整关闭路径（已修复）**
   - 增加 Escape 关闭和焦点返回；移动端增加背景遮罩及点击遮罩关闭，避免用户误操作被遮住的主内容。

5. **P2 — 命令补全缺少键盘退出与选中语义（已修复）**
   - Escape 可关闭补全但保留输入；补全项补充 `option` / `aria-selected`，输入框与补全列表建立 `aria-controls` 关系。

6. **P2 — 信息类系统消息看起来像错误（已修复）**
   - `/help` 等普通系统信息改为中性面板；请求失败、新会话失败和未知命令继续使用错误色，避免错误视觉被稀释。

### 本轮流程健康度

1. **离线草稿与本地命令：健康** — 输入、命令补全、Escape 和发送条件均已实际操作验证。
2. **操作反馈：健康** — 即时提示可见、可关闭，并同步进入右侧“最近操作”。
3. **长回复阅读：健康** — 输入区固定，独立滚动与“跳到最新消息”完成可见状态和滚动位置回读。
4. **右侧面板：桌面健康，移动端需设备补验** — 桌面 Escape 与焦点返回已验证；移动端遮罩为源码和构建验证，当前浏览器表面无法调整视口尺寸。
5. **模型与授权：受凭据限制** — 未改变业务协议；实际切换仍需在模型会话成功建立后补端到端验收。
